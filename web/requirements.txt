fastapi
uvicorn[standard]
python-multipart
anthropic
openai
python-pptx
pywin32; sys_platform == "win32"
pywinpty; sys_platform == "win32"
pystray; sys_platform == "win32"
Pillow
httpx
truststore  # route Python TLS through OS trust store (picks up corporate root CAs)
requests
mcp>=1.27.2,<2  # floor: 1.26.x raises spurious "Session terminated" on stateless servers (Nabu). ceiling: mcp 2.0 renamed McpError->MCPError (breaking) - our code (web/mcp/generic_client.py) uses McpError, so stay on 1.x
aiosqlite
browser-use==0.11.13  # pinned - was unconstrained and had silently drifted past our reset()/BrowserProfile usage (removed the force= kwarg on reset(), see browser_agent.py's _safe_reset_session)
litellm<1.92.0  # transitive via browser-use, unpinned - 1.92.0+ dropped Windows wheels entirely (Linux-only manylinux builds, a Rust/maturin component), forcing a source build that needs Rust and fails hard behind a TLS-inspecting corporate proxy. We don't use litellm directly.
playwright-stealth
plyer
apscheduler>=3.10,<4
sqlalchemy
packaging>=24.0
cairosvg
openpyxl>=3.1.0
python-docx
lxml>=5.0.0
ddgs>=9.0.0  # zero-key DuckDuckGo search — used by fetch_webpage 403 fallback
psutil>=5.9  # reliable pid liveness + port→pid for the opencode registry; without it _pid_alive shells out to tasklist and flashes console windows in the packaged app
