# Canonical requirements are in web/requirements.txt
# This file redirects to it so both `pip install -r requirements.txt`
# and `pip install -r web/requirements.txt` work identically.
-r web/requirements.txt
